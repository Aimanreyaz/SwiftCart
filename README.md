1.যদি কোনো ভ্যারিয়েবল শুধু declare করা হয় কিন্তু মান দেওয়া না হয়, তাহলে সেটা undefined থাকে।
কিন্তু null  ইচ্ছাকৃতভাবে সেট করে।
2.কোনো কিছু return করতে হলে map ব্যবহার করা হয়, আর কোনো কিছু return না করে শুধু দেখার বা কাজ করার জন্য forEach ব্যবহার করা হয়।
3.== Type চেক করে না, শুধু মান (value) চেক করে , === → Type চেক করে এবং মানও চেক করে
4. Callback বা .then() চেইনের দরকার নেই , সহজে step-by-step API call handle করা যায় , try/catch দিয়ে সহজে error handle করা যায়
5.global scope :ভ্যারিয়েবল ফাংশনের বাইরে ডিক্লেয়ার করলে সেটা সব জায়গা থেকে access করা যায়।
fuction: ভ্যারিয়েবল যদি ফাংশনের ভিতরে declare করা হয়, সেটা শুধু সেই ফাংশনের ভিতরে অ্যাক্সেস করা যায়।ফাংশনের বাইরে ব্যবহার করলে error হবে।
block:{} block এর বাইরে access করলে error হবে 